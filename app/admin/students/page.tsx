import { StudentsViewer } from '@/components/admin/students-viewer';

export const metadata = {
  title: 'Students - Aptitude Portal',
  description: 'View all students',
};

export default function StudentsPage() {
  return <StudentsViewer />;
}
