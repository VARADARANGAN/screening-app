import { StudentDashboard } from '@/components/student/dashboard';

export const metadata = {
  title: 'Dashboard - Aptitude Portal',
  description: 'Student dashboard',
};

export default function DashboardPage() {
  return <StudentDashboard />;
}
